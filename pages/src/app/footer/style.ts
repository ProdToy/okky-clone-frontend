import styled from 'styled-components'

export const FooterWrapper = styled.footer`
    display: flex;
    padding: 28px 0;
    justify-content: center;
    max-height: 170px;
    border-top: 1px solid var(--divider-main);
`

export const FooterContentField = styled.div`
    display: flex;
    width: 100%;
    max-width: 1280px;
`
